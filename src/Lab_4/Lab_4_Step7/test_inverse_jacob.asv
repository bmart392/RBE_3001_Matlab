clc; close all; clear all;
a = jacob0([0;0;0]); b = a(1:3,:); c = pinv(b);
disp(" This is b: "); disp(b);

disp(" This is c: "); disp(c);
% Creating the robot structure.
Robot.l1 = 0.135;
Robot.l2 = 0.175;
Robot.l3  = 0.16928;

finalForwardKinematics = [0.1; 0; 0.2];
currentForward = forward_kinematics([0;0;0]);
currentForwardKinematics = currentForward(1:3,:);

deltaBeforeThresh = b*(finalForwardKinematics - currentForwardKinematics);
disp(" delta Q: "); disp(deltaBeforeThresh);